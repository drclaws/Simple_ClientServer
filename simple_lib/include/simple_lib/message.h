#pragma once

#include <string>

#include "common.h"

namespace simpleApp
{
    const size_t MESSAGE_MAX_BUFFER = 1000;
    /*std::wstring msgTextToString();

    udp_header getUdpHeader();
    
    bool textToMsg(std::string text, char* buff, size_t buffsize);*/
}
